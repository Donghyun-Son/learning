#조건문 심화 예제 - 회원가입 id, password 만들기
#id 길이가 10을 넘으면 '회원가입 실패: id 길이가 10을 초과' 출력
#password 길이가 10을 넘으면 '회원가입 실패: password 길이가 10을 초과' 출력

id = input("id?")
pw = input("password?")

if len(id)<10:
    if len(pw)<10:
        print("회원가입 성공")
    else:
        print("회원가입 실패: password 길이가 10을 초과")
else:
    print("회원가입 실패: id 길이가 10을 초과")
